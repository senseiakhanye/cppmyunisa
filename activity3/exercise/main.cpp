#include <iostream>

using namespace std;

int main()
{
    int x, y,z ;
    cout << "Enter x y and z : " << endl;
    cin >> x >> y >> z;
    cout << "y * (z / x + 2 ) is " << y * (z / x + 2) << endl;
}